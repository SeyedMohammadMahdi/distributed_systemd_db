syntax = "proto3";

option go_package = "./grpc_util";

package grpc_util;

service PutLog {
    rpc PutOperation (Operation) returns (Status) {}
}

message Operation {
    string key = 1;
    string value = 2;
}

message Status {
    int64 status = 1;
}